# Definitions

I'll try to list all my definitions here.

## Addition of Fractions
### fadd 
```
((a)/(b)) + ((c)/(d)) = ((a)(d) + (c)(b)) / ((b)(d))
  fadd |\((.)/(.)\)[+]\((.)/(.)\)| ((\1*\4+\3*\2)/(\2*\4))
  fadd: |\((.)/(.)\)[+]\((.)/(.)\)| --> ((\1*\4+\3*\2)/(\2*\4)) # define
  (((a/b)+(c/d)))
  ((a*d+c*b)/(b*d)) # fadd
  ((((a+b)/c)+((x+y)/z)))
  (((a+b)*z+(x+y)*c)/(c*z)) # fadd
  ((((a+b)/(c+d))+((i+j)/(k+l))))
  (((a+b)*(k+l)+(i+j)*(c+d))/((c+d)*(k+l))) # fadd
```

## Multication of Fractions
### fmult
```
((a)/(b)) * ((c)/(d)) = (((a)(c))/((b)(d)))
  fmult \(\(([^()]+)\)/\(([^()]+)\)\)\*\(\(([^()]+)\)/\(([^()]+)\)\) (((\1)(\3))/((\2)(\4)))
  fmult: \(\(([^()]+)\)/\(([^()]+)\)\)\*\(\(([^()]+)\)/\(([^()]+)\)\) --> (((\1)(\3))/((\2)(\4))) # define
  ((a)/(b))*((c)/(d))
  (((a)(c))/((b)(d))) # fmult
```

## Multication of groupings
### pxp
```
(a+b)(c+d) = (ac+ad+bc+bd)
  pxp \(([^()]+)\+([^()]+)\)\(([^()]+)\+([^()]+)\) (\1\3+\1\4+\2\3+\2\4)
  pxp: \(([^()]+)\+([^()]+)\)\(([^()]+)\+([^()]+)\) --> (\1\3+\1\4+\2\3+\2\4) # define
  (a+b)(c+d)
  (ac+ad+bc+bd) # pxp
```
### mxppp
```
(a-b)(c+d+e+f) = (ac+ad+ae+af-bc-bd-be-bf)
  mxppp \(([^()]+)\-([^()]+)\)\(([^()]+)\+([^()]+)\+([^()]+)\+([^()]+)\) (\1\3+\1\4+\1\5+\1\6-\2\3-\2\4-\2\5-\2\6)
  mxppp: \(([^()]+)\-([^()]+)\)\(([^()]+)\+([^()]+)\+([^()]+)\+([^()]+)\) --> (\1\3+\1\4+\1\5+\1\6-\2\3-\2\4-\2\5-\2\6) # define
  (a-b)(c+d+e+f)
  (ac+ad+ae+af-bc-bd-be-bf) # mxppp
```
### mxm
```
(a-b)(c-d) = (ac-ad-bc+bd)
  mxm \(([^()]+)\-([^()]+)\)\(([^()]+)\-([^()]+)\) (\1\3-\1\4-\2\3+\2\4)
  mxm: \(([^()]+)\-([^()]+)\)\(([^()]+)\-([^()]+)\) --> (\1\3-\1\4-\2\3+\2\4) # define
  (a-b)(c-d)
  (ac-ad-bc+bd) # mxm
```
### mxmmp
```
(a-b)(c-d-e+f) = (ac-ad-ae+af-bc+bd+be-bf)
  mxmmp \(([^()]+)\-([^()]+)\)\(([^()]+)\-([^()]+)\-([^()]+)\+([^()]+)\) (\1\3-\1\4-\1\5+\1\6-\2\3+\2\4+\2\5-\2\6)
  mxmmp: \(([^()]+)\-([^()]+)\)\(([^()]+)\-([^()]+)\-([^()]+)\+([^()]+)\) --> (\1\3-\1\4-\1\5+\1\6-\2\3+\2\4+\2\5-\2\6) # define
  (a-b)(c-d-e+f)
  (ac-ad-ae+af-bc+bd+be-bf) # mxmmp
```

## Squares and Square Roots
### ww0
```
a^2 :: aa
  ww0 \b(\w)\^2\b \1\1
  ww0: \b(\w)\^2\b --> \1\1 # define
  a^2
  aa # ww0
```
### ww1
```
(a)^2 :: (a)(a)
  ww1 \(([^()]+)\)\^2\b (\1)(\1)
  ww1: \(([^()]+)\)\^2\b --> (\1)(\1) # define
  (a)^2
  (a)(a) # ww1
```

## Binomials
### bp0
```
(a+b)^2 = (a^2+2ab+b^2)
  bp0 \((\w+)\+(\w+)\)\^2\b (\1^2+2\1\2+\2^2)
  bp0: \((\w+)\+(\w+)\)\^2\b --> (\1^2+2\1\2+\2^2) # define
  (a+b)^2
  (a^2+2ab+b^2) # bp0
```
### bp1
```
((a)+(b))^2 = ((a)^2+2(a)(b)+(b)^2)
  bp1 \(\(([^()]+)\)\+\(([^()]+)\)\)\^2 ((\1)^2+2(\1)(\2)+(\2)^2)
  bp1: \(\(([^()]+)\)\+\(([^()]+)\)\)\^2 --> ((\1)^2+2(\1)(\2)+(\2)^2) # define
  ((a)+(b))^2
  ((a)^2+2(a)(b)+(b)^2) # bp1
```
### bm0
```
(a-b)^2 = (a^2-2ab+b^2)
  bm0 \((\w+)-(\w+)\)\^2\b ((\1)^2-2(\1)(\2)+(\2)^2)
  bm0: \((\w+)-(\w+)\)\^2\b --> ((\1)^2-2(\1)(\2)+(\2)^2) # define
  (a-b)^2
  ((a)^2-2(a)(b)+(b)^2) # bm0
```
### bm1
```
((a)-(b))^2 = ((a)^2-2(a)(b)+(b)^2)
  bm1 \(\(([^()]+)\)-\(([^()]+)\)\)\^2 ((\1)^2-2(\1)(\2)+(\2)^2)
  bm1: \(\(([^()]+)\)-\(([^()]+)\)\)\^2 --> ((\1)^2-2(\1)(\2)+(\2)^2) # define
  ((a)-(b))^2
  ((a)^2-2(a)(b)+(b)^2) # bm1
```

## Zeroes
### cancellations
```
+a-a = +0
  cancellations \+(\w+)\-\1(?=[+\-\)]) +0
  cancellations: \+(\w+)\-\1(?=[+\-\)]) --> +0 # define
  qp+abc-abc+xyz+i-i-ijk
  qp+0+xyz+0-ijk # cancellations
```

## Complex Numbers
### cdot0
```
(v.w) :: (v1w1+v2w2) # Dot Product
  cdot0 \((\w)\.(\w)\) (\11\21+\12\22)
  cdot0: \((\w)\.(\w)\) --> (\11\21+\12\22) # define
  (v.w)
  (v1w1+v2w2) # cdot0
```
### cdot1
```
((a,b).(c,d)) :: ((a)(c)+(b)(d)) # Dot Product
  cdot1 \(\(([^,]+),([^,]+)\)\.\(([^,]+),([^,]+)\)\) ((a)(c)+(b)(d))
  cdot1: \(\(([^,]+),([^,]+)\)\.\(([^,]+),([^,]+)\)\) --> ((a)(c)+(b)(d)) # define
  ((a,b).(c,d))
  ((a)(c)+(b)(d)) # cdot1
```
### cdet0
```
(v|w) :: (v1w2-v2w1) # Determinant
  cdet0 \((\w)\|(\w)\) (\11\22-\12\21)
  cdet0: \((\w)\|(\w)\) --> (\11\22-\12\21) # define
  (v|w)
  (v1w2-v2w1) # cdet0
```
### cdet1
```
((a,b)|(c,d)) :: ((a)(d)-(b)(c)) # Determinant
  cdet1 \(\(([^,]+),([^,]+)\)\|\(([^,]+),([^,]+)\)\) ((\1)(\4)-(\2)(\3))
  cdet1: \(\(([^,]+),([^,]+)\)\|\(([^,]+),([^,]+)\)\) --> ((\1)(\4)-(\2)(\3)) # define
  ((a,b)|(c,d))
  ((a)(d)-(b)(c)) # cdet1
```
### cadd0
```
(v+w) :: (v1+w1,v2+w2)
  cadd0 \((\w)\+(\w)\) (\11+\21,\12+\22)
  cadd0: \((\w)\+(\w)\) --> (\11+\21,\12+\22) # define
  (v+w)
  (v1+w1,v2+w2) # cadd0
```
### cadd1
```
((a,b)+(c,d)) :: ((a)+(c),(b)+(d))
  add1 \(\(([^,]+),([^,]+)\)\+\(([^,]+),([^,]+)\)\) ((\1)+(\3),(\2)+(\4))
  add1: \(\(([^,]+),([^,]+)\)\+\(([^,]+),([^,]+)\)\) --> ((\1)+(\3),(\2)+(\4)) # define
  ((a,b)+(c,d))
  ((a)+(c),(b)+(d)) # add1
```
### cmult0
```
(v*w) :: (v.w', v'|w) # Multiplication
  cmult0 \((\w)\*(\w)\) (\1.\2',\1'|\2)
  cmult0: \((\w)\*(\w)\) --> (\1.\2',\1'|\2) # define
  (v*w)
  (v.w',v'|w) # cmult0
```
### cmult1
```
((a,b)*(c,d)) :: ((a)(c)-(b)(d), (a)(d)+(b)(c))
  cmult1 \(\(([^,]+),([^,]+)\)\*\(([^,]+),([^,]+)\)\) ((\1)(\3)-(\2)(\4),(\1)(\4)+(\2)(\3))
  cmult1: \(\(([^,]+),([^,]+)\)\*\(([^,]+),([^,]+)\)\) --> ((\1)(\3)-(\2)(\4),(\1)(\4)+(\2)(\3)) # define
  ((a,b)*(c,d))
  ((a)(c)-(b)(d),(a)(d)+(b)(c)) # cmult1
```
### cturn0
```
(v^w) :: ((v|w)/(v.w)) # Turn (or Slope)
  cturn0 \((\w)\^(\w)\) ((\1|\2)/(\1.\2))
  cturn0: \((\w)\^(\w)\) --> ((\1|\2)/(\1.\2)) # define
  (v^w)
  ((v|w)/(v.w)) # cturn0
```

## Space Vectors
### vdot0
```
(v.w) :: (v1w1+v2w2+v3w3) # Dot Product
  vdot0 \((\w)\.(\w)\) (\11\21+\12\22+\13\23)
  vdot0: \((\w)\.(\w)\) --> (\11\21+\12\22+\13\23) # define
  (v.w)
  (v1w1+v2w2+v3w3) # vdot0
```
### vdot1
```
((a,b,c).(d,e,f)) :: ((a)(d)+(b)(e)+(c)(f)) # Dot Product
  vdot1 \(\(([^,]+),([^,]+),([^,]+)\)\.\(([^,]+),([^,]+),([^,]+)\)\) ((a)(d)+(b)(e)+(c)(f))
  vdot1: \(\(([^,]+),([^,]+),([^,]+)\)\.\(([^,]+),([^,]+),([^,]+)\)\) --> ((a)(d)+(b)(e)+(c)(f)) # define
  ((a,b,c).(d,e,f))
  ((a)(d)+(b)(e)+(c)(f)) # vdot1
```
### vdotx
```
(v.w) :: (vxwx+vywy+vzwz) # Dot Product
  vdotx \((\w)\.(\w)\) (\1x\2x+\1y\2y+\1z\2z)
  vdotx: \((\w)\.(\w)\) --> (\1x\2x+\1y\2y+\1z\2z) # define
  (v.w)
  (vxwx+vywy+vzwz) # vdotx
```
### vdotd
```
(v1.v2) :: (x1x2+y1y2+z1z2) # Dot Product
  vdotd \((\w)(\d+)\.\1(\d+)\) (x\2x\3+y\2y\3+z\2z\3)
  vdotd: \((\w)(\d+)\.\1(\d+)\) --> (x\2x\3+y\2y\3+z\2z\3) # define
 (v1.v2)
 (x1x2+y1y2+z1z2) # vdotd
```
### vx0
```
(v*w) :: (v2w3-w2v3,v3w1-w3v1,v1w2-w1v2) # Cross Product
  vx0 \((\w)\*(\w)\) (\12\23-\22\13,\13\21-\23\11,\11\22-\21\12)
  vx0: \((\w)\*(\w)\) --> (\12\23-\22\13,\13\21-\23\11,\11\22-\21\12) # define
  (v*w)
  (v2wu-w2v3,v3w1-w3v1,v1w2-w1v2) # vx0
```
### vx1
```
((a,b,c)*(d,e,f)) :: ((b)(f)-(e)(c),(c)(d)-(f)(a),(a)(e)-(d)(b)) # Cross Product
  vx1 \(\(([^,]+),([^,]+),([^,]+)\)\*\(([^,]+),([^,]+),([^,]+)\)\) ((\2)(\6)-(\5)(\3),(\3)(\4)-(\6)(\1),(\1)(\5)-(\4)(\2))
  vx1: \(\(([^,]+),([^,]+),([^,]+)\)\*\(([^,]+),([^,]+),([^,]+)\)\) --> ((\2)(\6)-(\5)(\3),(\3)(\4)-(\6)(\1),(\1)(\5)-(\4)(\2)) # define
  ((a,b,c)*(d,e,f))
  ((b)(f)-(e)(c),(c)(d)-(f)(a),(a)(e)-(d)(b)) # vx1
```
### vxd
```
(v1*v2) :: (y1z2-y2z1, z1x2-z2x1, x1y2-x2y1)
  vxd \((\w)(\d+)\*\1(\d+)\) (y\2z\3-y\3z\2,z\2x\3-z\3x\2,x\2y\3-x\3y\2)
  vxd: \((\w)(\d+)\*\1(\d+)\) --> (y\2z\3-y\3z\2,z\2x\3-z\3x\2,x\2y\3-x\3y\2) # define
  (v1*v2)
  (y1z2-y2z1,z1x2-z2x1,x1y2-x2y1) # vxd
```
